/**
 * Application configuration for URLs and other app-wide settings.
 */
export const appConfig = {
  /**
   * Base URL for public profile pages.
   * This is used to generate QR codes and shareable links.
   */
  publicProfileBaseUrl: "https://rallyhub.io/p",
} as const;
